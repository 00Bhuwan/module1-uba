# help multiple files with individual function to work as a single file: module
# a package is a folder that store modules 
# common practice is to make __init__.py file inside a package folder

from _26_oop import Addition
add = Addition(2, 4)
add.get_val()
add.add()
